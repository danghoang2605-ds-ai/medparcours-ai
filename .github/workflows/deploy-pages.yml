name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install esbuild@0.27.7 react@19.2.6 react-dom@19.2.6

      - name: Prepare build folder
        run: |
          mkdir -p build
          cp App.jsx build/App.jsx
          cp docker_setup/mount.jsx build/mount.jsx

      - name: Build bundle
        run: |
          mkdir -p dist
          npx esbuild build/mount.jsx --bundle --loader:.jsx=jsx --jsx=automatic --format=esm --outfile=dist/mount.js --minify

      - name: Copy index.html
        run: cp index.html dist/index.html

      - name: Copy logos folder
        run: |
          mkdir -p dist/logos
          cp -r logos/. dist/logos/ || true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
