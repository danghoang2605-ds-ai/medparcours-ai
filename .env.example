# Copy file này thành ".env" rồi điền API key thật trước khi chạy docker-compose.
# KHÔNG commit file .env thật lên Git (đã có trong .gitignore).
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
